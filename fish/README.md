# fish (deprecated)

This package has been replaced by the `zsh/` package.

Shell configuration now lives in `zsh/.config/zsh/` — see [../README.md](../README.md) for setup instructions.

If you previously stowed this package, unlink it first:

```bash
stow -D fish
```
